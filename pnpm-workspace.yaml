packages:
  - "apps/*"
  - "packages/*"
# Allow these deps to run install scripts (electron downloads its binary; esbuild its native build).
allowBuilds:
  electron: true
  electron-winstaller: false
  esbuild: true
