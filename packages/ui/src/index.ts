/**
 * @compass/ui — shared React component library (built on @compass/tokens).
 * Primitives (Button, Input, Field, Card, Modal, Spinner, Badge) land here so
 * features compose them instead of re-rolling styles. Populated as Phase 0 proceeds.
 */
export {};
